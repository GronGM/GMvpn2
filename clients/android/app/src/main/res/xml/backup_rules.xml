<?xml version="1.0" encoding="utf-8"?>
<!-- No backup of tunnel state or secrets. Profiles / keys live in
     encrypted storage that we deliberately exclude. -->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
